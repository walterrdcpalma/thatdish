import { View } from "react-native";

/**
 * Add tab placeholder. Navigation to create dish is handled by the tab bar button.
 */
export default function AddTab() {
  return <View className="flex-1" />;
}
