import { View, Text } from "react-native";

export default function SavedTab() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-gray-500">Saved (coming soon)</Text>
    </View>
  );
}
